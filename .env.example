# Copy to .env (do not commit .env) and fill values.

# WhatsApp Cloud API (Meta)
WHATSAPP_ENABLED=0
WHATSAPP_DRY_RUN=1
WHATSAPP_GRAPH_VERSION=v19.0
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=

# Approved template name + language code (must exist in WhatsApp Manager)
WHATSAPP_TEMPLATE_TASK_DONE=task_done
WHATSAPP_TEMPLATE_LANG=bg

# Used as 3rd template parameter (link back to the app)
PUBLIC_BASE_URL=http://127.0.0.1:3310
